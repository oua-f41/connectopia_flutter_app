import 'group.dart';
import 'user.dart';

class UserGroups {
  String? id;
  String? userId;
  String? groupId;
  Group? group;
  User? user;

  UserGroups({this.id, this.userId, this.groupId, this.group, this.user});
}
